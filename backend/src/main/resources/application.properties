# Database Configuration
spring.datasource.url=jdbc:mysql://bnhloi3s5fkd2wtunfdb-mysql.services.clever-cloud.com:3306/bnhloi3s5fkd2wtunfdb?useSSL=true&requireSSL=true
spring.datasource.username=ulzgr09aqlqzjz4m
spring.datasource.password=uLopCvGxyPjg90HmVEBv
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.jpa.properties.hibernate.format_sql=true

spring.main.banner-mode=console

# Email configuration (Gmail) - Disabled by default for deployment
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# JWT configuration
security.jwt.secret=${JWT_SECRET:default-secret-key-change-in-production}
security.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

server.port=${SERVER_PORT:8080}

# Flyway configuration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration
spring.flyway.validate-on-migrate=true
spring.flyway.out-of-order=false
spring.flyway.clean-disabled=true

# File upload configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# File storage configuration
app.upload.dir=${UPLOAD_DIR:/tmp/uploads}
app.upload.base-url=${UPLOAD_BASE_URL:http://localhost:8080/uploads}

# CORS configuration
app.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:3000,http://localhost:8080}
